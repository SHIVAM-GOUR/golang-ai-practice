package main

import (
	"log"
)

func main() {
	ch := make(chan int, 3)

	print(ch)

	// a := <-ch
	// log.Println(a)
	// b := <-ch
	// log.Println(b)
	// c := <-ch
	// log.Println(c)	

	close(ch);
	for v := range ch {
		log.Println(v)
	}

}

func print(ch chan int) {
	ch <- 1
	ch <- 2
	ch <- 3
}
